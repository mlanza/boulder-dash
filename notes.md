```js
function getEntities(state, [...components], [...touches]){ //returns ids for entities having components and touches matching some value e.g. `f`
}
const any = _.constantly(true);
getEntities({"controlled": any, "noun": updated, "position": touched});
```
